---
- name: Instalar herramientas
  hosts: kubernetes
  become: true
  become_user: root  
  
  tasks: 

  - name: generate SSH key
    openssh_keypair:
      path: "~/.ssh/id_ed25519"
      type: ed25519
      size: 4096
      state: present
      force: yes
